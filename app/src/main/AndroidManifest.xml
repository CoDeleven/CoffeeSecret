<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dhy.coffeesecret">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.device.DeviceChildActivity" />
        <activity android:name=".ui.container.BeanInfoActivity" />
        <activity android:name=".ui.container.EditBeanActivity" />
        <activity android:name=".ui.container.LinesSelectedActivity" />
        <activity android:name=".ui.device.fragments.ReportActivity" />
        <activity android:name=".ui.cup.NewCuppingActivity" />
        <activity android:name=".ui.container.SelectInfoActivity"></activity>
    </application>

</manifest>