<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dhy.coffeesecret">

    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.device.DeviceChildActivity" />
        <activity android:name=".ui.container.BeanInfoActivity" />
        <activity android:name=".ui.container.fragments.LinesSelectedActivity" />
        <activity android:name=".ui.device.fragments.ReportActivity" />
        <activity android:name=".ui.cup.NewCuppingActivity"></activity>
        <activity android:name=".ui.device.DialogBeanSelectedActivity">
        </activity>
        <activity android:name=".ui.mine.BluetoothListActivity">
        </activity>
    </application>

</manifest>